"""Controlled AI assistance: extraction v2, translation, summarization, hypothesis help (Phase 8, GOAL.md §26).

Every producer in this package emits *suggestions* into the review queue with
source references — never canonical writes (Article VII; the P8 exit criterion
is a test proving zero direct canonical writes from any AI code path).
Translations and transcripts are stored as evidence derivatives (Article IV).
"""
