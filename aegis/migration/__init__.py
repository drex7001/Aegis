"""One-time legacy adapters (T8, ADR-016) — the only place legacy vocabulary lives.

`legacy.py` will hold the verb-remap table and the ConfidenceTag→grading map
(specs/02 §6), both validated against the ontology registry at run time.
"""
