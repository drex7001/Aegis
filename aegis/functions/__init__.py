"""Functions registry: declared, versioned derivations over the ontology (Phase 3, spec 08 §4).

Kinetic-layer derivations — computed predicates (first: prison co-location,
``prison_overlap.py``), derived display values, rule-based inference. Output is
attributed to an algorithmic source and enters the store as a suggestion or an
explicit system claim, never as anonymous fact (Article VII, GOAL.md §7.8).
"""
