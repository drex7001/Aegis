"""Governed analytics: k-hop, paths, Leiden, brokerage, shared identifiers (Phase 6, GOAL.md §13).

Every result is an ``AnalyticFinding`` carrying method, parameters, and
interpretation caveats (Article IX — association is not guilt); findings are a
distinct type from claims and are promoted only through a human action.
Object sets (saved, composable, access-controlled queries) feed runs here.
"""
